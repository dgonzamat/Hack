# Python dependencies for hackingtool
# boxes and lolcat are system CLI tools, not pip packages — install via apt/brew
# flask is unused — removed
# requests is unused at runtime — removed
rich>=13.0.0

# PoC Cubicación AI
anthropic>=0.40,<1.0
pymupdf>=1.24
pillow>=10
openpyxl>=3.1
scikit-learn>=1.4
pyyaml>=6.0
pytest>=8.0

# scraper_precios.py — ejecutar solo en maquina local (red cloud bloqueada)
playwright>=1.44
curl_cffi>=0.7
